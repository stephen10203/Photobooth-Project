using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
using UnityEngine.Video;

public class ExampleVideoStream : MonoBehaviour
{
    //public VideoClip theVideo;

    // Use this for initialization
    public void Start()
    {
        //string path = GameObject.Find("PersistentManager").GetComponent<PersistentManager>().FolderPath() + "video.avi";
        ////string path = Application.dataPath + "/Outputs/Typing_example.avi";
        //Debug.Log(path);
        ////VideoClip theVideo = Resources.Load(path) as VideoClip;
        //VideoPlayer vp = gameObject.AddComponent<VideoPlayer>();
        //vp.url = path;
        //vp.source = VideoSource.Url;
        //vp.renderMode = VideoRenderMode.RenderTexture;
        //vp.targetTexture = new RenderTexture(100, 100, 0);
        //vp.isLooping = true;
        //vp.Play();

        //gameObject.GetComponent<RawImage>().texture = vp.targetTexture;
    }
}